version: "3.9"

services:

  mongodb:
    image: mongo:7

    container_name: nestbites-mongodb

    restart: always

    ports:
      - "27017:27017"

    volumes:
      - mongo-data:/data/db

volumes:

  mongo-data: